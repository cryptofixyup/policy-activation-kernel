interface:
  display_name: "Policy Activation Kernel"
  short_description: "Repo-specific patterns and workflows for policy-activation-kernel"
  default_prompt: "Use the policy-activation-kernel repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true