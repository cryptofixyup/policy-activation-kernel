# policy-activation-kernel
Closed, dependency-free reference kernel for policy-activation.v1 — canonical specification, semantic validator, pure execution engine, invariants, and deterministic transition vectors.
